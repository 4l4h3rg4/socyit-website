{
  "name": "socyit-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:admin": "pnpm --filter admin dev",
    "build:web": "pnpm --filter web build",
    "build:admin": "pnpm --filter admin build"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@tsparticles/engine",
      "esbuild",
      "sharp"
    ]
  }
}
