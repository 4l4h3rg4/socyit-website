---
// Unete.astro — CTA final con logo flotando y botón de WhatsApp

const whatsappUrl = import.meta.env.PUBLIC_WHATSAPP_URL ?? '#';

const pasos = [
  'Entra a la comunidad de WhatsApp',
  'Explora las divisiones que te interesan',
  'Colabora, aprende y construye proyectos',
  'Despega con nosotros 🚀',
];
---

<section id="unete" class="unete">
  <!-- Resplandor radial de fondo -->
  <div class="unete__glow" aria-hidden="true"></div>

  <div class="container unete__inner">

    <!-- Logo flotando — .reveal eliminado, GSAP maneja la entrada -->
    <div class="unete__logo-wrap">
      <img
        src="/logo.svg"
        alt="Logo SocyIT"
        class="unete__logo animate-float animate-glow-pulse"
        width="140"
        height="140"
      />
    </div>

    <!-- Título -->
    <h2 class="unete__title glow-text">
      &gt; Únete a la tripulación_
    </h2>

    <!-- Tarjeta de pasos -->
    <div class="unete__card">
      <ol class="unete__steps">
        {pasos.map((paso, i) => (
          <li class="unete__step">
            <span class="unete__step-num">{String(i + 1).padStart(2, '0')}</span>
            <span class="unete__step-text">{paso}</span>
          </li>
        ))}
      </ol>
    </div>

    <!-- Botón WhatsApp -->
    <a
      href={whatsappUrl}
      target="_blank"
      rel="noopener noreferrer"
      class="pixel-btn pixel-btn--whatsapp unete__btn"
      aria-label="Unirse a la comunidad SocyIT por WhatsApp"
    >
      &gt; Unirme por WhatsApp_
    </a>

    <!-- Texto inferior con cursor -->
    <p class="unete__footer-text cursor">
      Prepárate para despegar
    </p>

  </div>
</section>

<script>
  // ---- GSAP: logo pop + sequence glow ----
  import { gsap, ScrollTrigger, withMotion } from '../../lib/gsap';

  withMotion(() => {
    gsap.set('.unete__glow',        { autoAlpha: 0, scale: 0.5 });
    gsap.set('.unete__logo-wrap',   { autoAlpha: 0, scale: 0.55 });
    gsap.set('.unete__title',       { autoAlpha: 0, y: 22 });
    gsap.set('.unete__card',        { autoAlpha: 0, y: 22 });
    gsap.set('.unete__btn',         { autoAlpha: 0, y: 22 });
    gsap.set('.unete__footer-text', { autoAlpha: 0 });

    ScrollTrigger.create({
      trigger: '#unete',
      start: 'top 78%',
      once: true,
      onEnter: () => {
        const tl = gsap.timeline();
        tl.to('.unete__glow',        { autoAlpha: 0.7, scale: 1, duration: 0.9, ease: 'power2.out' })
          .to('.unete__logo-wrap',   { autoAlpha: 1, scale: 1, duration: 0.7, ease: 'back.out(2)' }, '<')
          .to('.unete__title',       { autoAlpha: 1, y: 0, duration: 0.5 }, '-=0.2')
          .to('.unete__card',        { autoAlpha: 1, y: 0, duration: 0.5 }, '-=0.2')
          .to('.unete__btn',         { autoAlpha: 1, y: 0, duration: 0.4 }, '-=0.15')
          .to('.unete__footer-text', { autoAlpha: 1, duration: 0.4 }, '-=0.1');
      },
    });
  });
</script>

<style>
  .unete {
    position: relative;
    z-index: 10;
    text-align: center;
    overflow: hidden;
  }

  /* Resplandor radial de fondo */
  .unete__glow {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, var(--primary-glow) 0%, transparent 65%);
    pointer-events: none;
    z-index: 0;
  }

  .unete__inner {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2rem;
    max-width: 600px;
    margin-inline: auto;
  }

  /* ---- Logo ---- */
  .unete__logo-wrap {
    margin-bottom: 0.5rem;
  }

  .unete__logo {
    width: 140px;
    height: 140px;
    object-fit: contain;
  }

  /* ---- Título ---- */
  .unete__title {
    font-family: var(--font-pixel);
    font-size: clamp(0.75rem, 3vw, 1.1rem);
    line-height: 1.5;
    color: var(--text);
  }

  /* ---- Tarjeta de pasos ---- */
  .unete__card {
    background: var(--bg-card);
    border: 1px solid var(--border-bright);
    border-top: 3px solid var(--primary);
    padding: 1.5rem 2rem;
    width: 100%;
  }

  .unete__steps {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    text-align: left;
  }

  .unete__step {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
  }

  .unete__step-num {
    font-family: var(--font-pixel);
    font-size: 0.5rem;
    color: var(--primary);
    flex-shrink: 0;
    margin-top: 0.15rem;
  }

  .unete__step-text {
    font-family: var(--font-mono);
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.6;
  }

  /* ---- Botón ---- */
  .unete__btn {
    font-size: 0.6rem !important;
    padding: 1rem 2rem !important;
  }

  /* ---- Texto con cursor ---- */
  .unete__footer-text {
    font-family: var(--font-pixel);
    font-size: 0.5rem;
    color: var(--text-dim);
    letter-spacing: 0.05em;
  }
</style>
