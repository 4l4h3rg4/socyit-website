---
import AdminLayout from '../../../../layouts/AdminLayout.astro';
import { createAdminClient } from '../../../../lib/supabase';

const { id } = Astro.params;
const supabase = createAdminClient(Astro);
const { data: lider } = await supabase.from('lideres').select('id, nombre').eq('id', id).single();
if (!lider) return Astro.redirect('/dashboard/lideres');
---

<AdminLayout title={`Eliminar: ${lider.nombre}`} activePage="lideres">
  <div class="admin-topbar">
    <h1>Eliminar Líder</h1>
    <a href="/dashboard/lideres" class="btn btn--ghost">[ Volver ]</a>
  </div>

  <div class="nes-container with-title" style="max-width:500px;">
    <p class="title">Confirmar eliminación</p>
    <p style="margin-bottom:1.5rem; font-size:0.875rem; color:var(--text-muted);">
      ¿Eliminar al líder <strong style="color:var(--text);">{lider.nombre}</strong>?
    </p>
    <div style="display:flex; gap:0.75rem;">
      <button id="btn-confirmar" class="btn btn--danger">[ Sí, eliminar ]</button>
      <a href="/dashboard/lideres" class="btn btn--ghost">Cancelar</a>
    </div>
    <p id="error" style="display:none; margin-top:1rem; font-family:var(--font-pixel); font-size:0.6rem; color:var(--danger);"></p>
  </div>

  <script define:vars={{ liderId: lider.id }}>
    document.getElementById('btn-confirmar')?.addEventListener('click', async () => {
      const errEl = document.getElementById('error');
      const res = await fetch(`/api/lideres/${liderId}`, { method: 'DELETE' });
      if (res.ok) {
        window.location.href = '/dashboard/lideres';
      } else {
        const json = await res.json().catch(() => ({}));
        errEl.textContent = json.error ?? 'Error al eliminar.';
        errEl.style.display = 'block';
      }
    });
  </script>
</AdminLayout>
